# Per-deployment secrets. Copy to collector/.env and fill in.
# scripts/bootstrap will generate INGEST_BEARER_TOKEN if it's empty.

export HONEYCOMB_API_KEY=
export HONEYCOMB_OTLP_ENDPOINT=https://api.honeycomb.io
export INGEST_BEARER_TOKEN=
